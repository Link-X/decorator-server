{
  "name": "@decorator-server/exp",
  "description": "a node package that can use decorator to create HTTP servers",
  "version": "0.12.0",
  "author": "simple-decorator",
  "main": "./lib",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "starter"
  },
  "dependencies": {
    "@decorator-server/decorator": "^0.12.0",
    "@decorator-server/starter": "^0.12.0"
  }
}
